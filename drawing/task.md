# task.md — こども おえかき 改修タスク

## 背景 / 現状
- 基本機能（描画・色・虹色・消しゴム・Undo/Redo・スタンプ・ぬりえ・保存・背景・バケツ・効果音・全画面）は実装済み。
- スマホ縦画面では動作確認済み。
- ただし **想定は iPad の横画面**。現状は色パレット・太さが常時表示で、描画スペースを圧迫している。

## 今回の修正要望
1. **iPad 横画面（ランドスケープ）での使用を主対象にレイアウト最適化する**
2. **描画スペースを最大化する** — 色・太さを変えるUIは常時表示をやめ、
   ボタンを押すと出てくる（ポップアップ）方式にする

## 方針
- 下部ツールバーを **1行に集約**（常時表示の色12個・太さ4個の行を廃止）。
- 「いろ」ボタン・「ふとさ」ボタンを新設。押すとポップアップで選べる。
  - ボタンには現在の色・太さをプレビュー表示。
- ポップアップ表示中はキャンバスをタップしても描画せず、ポップアップを閉じる（誤描画防止）。
- 横画面で広く使えるよう、ステージ余白を最小化。縦画面でも崩れないようにする。

## やること（チェックリスト）
- [x] task.md 作成
- [x] index.html: 下部バーを単一ツールバーへ。色/太さの常時行を削除
- [x] style.css: 1行ツールバー / 色・太さプレビューボタン / 横画面チューニング / ステージ余白最小化
- [x] app.js: ツールバー生成を tools + 色ボタン + 太さボタン に変更
- [x] app.js: 色パネル・太さパネルのポップアップを実装（選んだら閉じる・プレビュー更新）
- [x] app.js: パネル表示中はキャンバスのタップで描画せず閉じる
- [x] app.js: パネルの開閉トグル（同じボタン再押下で閉じる）
- [x] 構文チェック（node --check）
- [ ] 実機/ブラウザでの動作確認（iPad 横画面想定 / 縦画面でも崩れないこと）← 要・お試し

## 実装メモ（今回の変更）
- 下部バーは 1 行に集約。iPad 横では 1 行、狭い画面では折り返して全ボタン表示。
- 「いろ」「ふとさ」ボタンを新設し、押すとポップアップ表示（現在値はボタンにプレビュー）。
- ポップアップはツールバーの真上に出す（`bottom: calc(100% + 4px)` でバー高さに追従）。
- ポップアップ表示中はキャンバスのタップで描画せず閉じる（誤描画防止）。
- 同じボタンの再押下でポップアップを閉じるトグル動作。
- ステージ余白を 8px→6px に縮小し、横画面で薄いバー（ラベル非表示）にして描画領域を拡大。

## メモ / 今後
- 色・スタンプ・ぬりえ・背景は js/data.js のデータ追加だけで増やせる構成は維持。
- iPad 実機の Safari で、横画面のボタン配置・ポップアップ位置を最終確認したい。

---

# 追加タスク（2026-06-20）: イラスト背景をたくさん追加

## 要望
- 「はいけい」に、子供が楽しめるイラスト背景を多数追加。
  例: 公園 / 遊園地 / ちょっと怖い夜 など。
  背景の上にボール・人・動物などを描いて遊べるようにする。

## 方針
- 外部画像は使わず（オフライン維持）、SVG をコードで描いて背景にする。
- 保守用に専用ファイル `js/scenes.js` を新設し、`data.js` から取り込む。
- キャンバスは cover 表示（画面いっぱい・歪ませない）。中央は描画用に広く空ける。

## やること
- [x] js/scenes.js 新設（公園/遊園地/うみ/うちゅう/まち/ぼくじょう/ゆき/おばけ）
- [x] data.js: SCENES を読み込み BACKGROUNDS の先頭へ
- [x] canvas.js: type 'scene' を cover 描画 + 非同期読込対応
- [x] app.js: はいけいパネルのサムネイルをシーン表示に
- [x] css: シーンサムネイル + ラベル
- [x] service-worker.js: scenes.js を追加しキャッシュ更新
- [x] 構文チェック / scenes.js 実行検証 / SVG妥当性チェック
- [x] GitHub へ反映（c89acc2）

---

# 追加タスク（2026-06-20）: iPad の誤操作対策

## 要望
- お絵描き中に2〜3本指スワイプ・下/上からのスワイプで中断してしまう。
- ホームボタン機なので iOS のスワイプ機能は無効化されても良い。

## 対策（2系統）
### アプリ側（実装）
- [x] body を position:fixed にしてラバーバンド(引っぱり)を停止
- [x] ピンチ拡大(gesturestart等)を preventDefault
- [x] touchmove を抑制（パネル内の縦スクロールだけ pan-y で許可）
- [x] 長押しメニュー(callout/contextmenu)を抑制
- [x] service-worker キャッシュ v4
- [x] 構文チェック

### iOS 側（ユーザー操作・READMEに手順追記）
- Web では止められないシステムスワイプ（コントロールセンター/Dock/アプリ切替）対策。
- 「アクセスガイド」+「ホーム画面に追加」を案内。
- [x] README に手順を追記
- [x] GitHub へ反映（3ec3adf）

---

# 追加タスク（2026-06-20）: ぬりえバグ修正 + スタンプ大幅追加

## バグ
- ぬりえを開くと枠内に図形が出ない（iOS）。
- 原因: ぬりえSVGに width/height が無く、iOS Safari が寸法なしSVGを canvas に描けない。
- [x] wrapSvg に width="100" height="100" を付与して修正

## スタンプ追加
- 人間（全身）・動物（全身）を中心に大幅追加。おままごと遊び向け。
- [x] data.js を STAMP_GROUPS（カテゴリ別）に再構成、STAMPS は平坦配列(ランダム用)
- [x] app.js: スタンプパネルにカテゴリ見出しを表示
- [x] css: .panel-section（グリッド全幅の見出し）
- [x] service-worker キャッシュ v5
- [x] 検証（合計108個・重複なし・テンプレ寸法あり）
- [x] GitHub へ反映（f7ea134）

---

# 追加タスク（2026-06-20）: スタンプパネルが重なる不具合

## 不具合
- 108個を詰め込んだ結果、パネルが内部スクロールせず溢れ、スタンプが重なって見えない。

## 修正
- [x] .panel-body に flex:1 / min-height:0 / align-content:start（flex子要素のスクロール有効化）
- [x] .panel max-height 56vh→70vh（表示領域拡大）
- [x] .pitem に min-height:64px（aspect-ratio未対応の古いiOSでも潰れない保険）
- [x] service-worker キャッシュ v6
- [ ] GitHub へ反映
